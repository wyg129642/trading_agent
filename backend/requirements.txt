# FastAPI & Server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Database
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.30.0
psycopg2-binary>=2.9.0
alembic>=1.14.0

# Redis
redis[hiredis]>=5.2.0

# Auth
PyJWT>=2.9.0
bcrypt>=4.2.0

# Validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# HTTP
httpx>=0.27.0
google-genai>=1.0.0
aiohttp>=3.9.0

# Engine dependencies (shared with engine/)
pyyaml>=6.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
feedparser>=6.0.0
openai>=1.30.0
zai-sdk>=0.2.2
trafilatura>=1.6.0
chardet>=5.0.0
aiosqlite>=0.19.0
duckduckgo-search>=6.0.0
tiktoken>=0.7.0
playwright==1.49.1
python-dateutil>=2.8.0
clickhouse-connect>=0.7.0
structlog>=24.0.0

# Stock data
akshare>=1.14.0
yfinance>=0.2.50
pandas>=2.0.0
futu-api>=9.0.0   # Futu OpenAPI — primary quote source (HK/A-share/US via local FutuOpenD)

# MongoDB (AlphaPai crawl data)
motor>=3.5.0
pymongo>=4.8.0

# MySQL (Wind 一致预期 consensus forecast for dashboard 持仓概览)
pymysql>=1.1.0

# Vector retrieval (Phase B hybrid RAG — kb_search internals)
# Milvus 2.5.x is required for Function API (built-in BM25). pymilvus 2.5.x matches.
pymilvus>=2.5.0,<2.7
langchain-text-splitters>=0.3.0   # Markdown-aware + recursive char splitter

# Personal knowledge base — file parsers for user-uploaded documents.
# opendataloader-pdf is Java-backed (requires a JDK 11+ on $PATH) and produces
# structure-preserving markdown; pypdf is a pure-Python fallback used when the
# JVM path fails. python-docx adds .docx support.
opendataloader-pdf>=2.2.0
pypdf>=4.0.0
python-docx>=1.1.0
# Chinese segmentation for the personal-KB $text index. Without this,
# MongoDB treats a whole Chinese chunk as one atomic "token" and queries
# like "接口说明" match nothing.
jieba>=0.42.1
